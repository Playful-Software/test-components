{
  "name": "qrcode",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "node build.js && cp src/qrcode.min.js ${BUILD_DIR:-build} "
  },
  "dependencies": {},
  "peerDependencies": {},
  "devDependencies": {
    "esbuild": "^0.9.3"
  }
}
